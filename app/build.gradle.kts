import java.io.FileInputStreamimport java.util.Propertiesplugins {    id("com.android.application")    id("org.jetbrains.kotlin.android")}// Release signing is read from an UNTRACKED keystore.properties at the repo root (see// keystore.properties.example). If it's absent, `release` builds unsigned — Studio's// "Generate Signed Bundle" still works independently.val keystorePropsFile = rootProject.file("keystore.properties")val keystoreProps = Properties().apply { if (keystorePropsFile.exists()) load(FileInputStream(keystorePropsFile)) }android {    namespace = "com.openzeekr.app"    compileSdk = 34    defaultConfig {        applicationId = "com.openzeekr.app"        minSdk = 26        targetSdk = 34        versionCode = 36        versionName = "0.1.19"        // App-global secrets are baked in :core (SecretsConfig + BuildConfig live there).    }    signingConfigs {        if (keystorePropsFile.exists()) create("release") {            storeFile = file(keystoreProps.getProperty("storeFile"))            storePassword = keystoreProps.getProperty("storePassword")            keyAlias = keystoreProps.getProperty("keyAlias")            keyPassword = keystoreProps.getProperty("keyPassword")            keystoreProps.getProperty("storeType")?.let { storeType = it } // e.g. PKCS12 for a .p12        }    }    buildTypes {        debug {            // Test build installs beside the upstream release while phone and watch retain the same            // package id/signing identity required by the Wear Data Layer.            applicationIdSuffix = ".test"            versionNameSuffix = "-work"            // CI writes keystore.properties from the encrypted permanent test-key secret. Binding            // it explicitly prevents AGP from silently generating a new debug identity.            if (keystorePropsFile.exists()) signingConfig = signingConfigs.getByName("release")        }        release {            isMinifyEnabled = true            isShrinkResources = true            proguardFiles(getDefaultProguardFile("proguard-android-optimize.txt"), "proguard-rules.pro")            if (keystorePropsFile.exists()) signingConfig = signingConfigs.getByName("release")        }    }    compileOptions {        sourceCompatibility = JavaVersion.VERSION_17        targetCompatibility = JavaVersion.VERSION_17    }    kotlinOptions {        jvmTarget = "17"
